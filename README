Smlr is a simple URL shortener for the web.

To set up a development environment...
 * Download
 * Run 'python bootstrap.py'
 * Run './bin/buildout'
 * Setup database
 * Change settings.py database credentials or add to settings_local.py
 * Change settings.py SECRET_KEY, templates, static etc. settings or override settings_local.py
 * Run `./bin/django syncdb'
 * Run `./bin/django runserver'
